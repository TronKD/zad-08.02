#include <iostream>
#include <fstream>

using namespace std;

int main() {
    int i;
    int temp;
    int liczba1;
    int cyfr6d;
    

    ifstream plik1;
    plik1.open("liczby2.txt");

    for(i=0; i<1000; i++) {
        plik1 >> dec >> liczba1;
  	  	temp = liczba1;
 		while (temp) {
 			if (temp%10 == 6) {
 				cyfr6d++;
 			}
 		temp /= 10;
 		}
    }
    plik1.close();

    cout << "cyfra 6 wy systemie dec:  " << cyfr6d ;

    return 0;
}
